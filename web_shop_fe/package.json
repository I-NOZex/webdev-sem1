{
  "name": "web_shop_fe",
  "version": "0.0.0",
  "scripts": {
    "dev": "start cmd /k json-server --watch ../db.json --port=8008 & vite",
    "build": "vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.6.13"
  }
}
